---
type Props = {
  skills: string[];
}

const {skills,} = Astro.props;
---

{skills.map((skill) => {
  return <li class="hover:scale-130 cursor-default transform duration-200 font-text h-auto sm:h-6  md:h-12 lg:h-24 hover:text-button-active">{skill}</li>
})}